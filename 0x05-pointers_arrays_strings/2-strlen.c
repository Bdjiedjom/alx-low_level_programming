#include <string.h>

/**
 * _strlen - return the length of a string give as parametre
 * @s: character pointer that point to the string
 *
 * Return: 0
 */

int _strlen(char *s)
{
	int len;

	len = strlen(s);
	return (len);
}
